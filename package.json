{
  "name": "school-bank-saas",
  "private": true,
  "version": "1.0.0",
  "description": "School Bank SaaS: Core Banking API (Node/SQLite) + Dashboard (React/Vite)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix banking-dashboard",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "dev:dashboard": "npm run dev --prefix banking-dashboard",
    "build": "npm run build --prefix backend && npm run build --prefix banking-dashboard"
  }
}
